{
  "name": "sleepy-tabs-companion",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "bin": {
    "sleepy-tabs-companion": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint --ext .ts src",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "tsx": "^4.15.7",
    "typescript": "5.5.4",
    "vitest": "^2.0.5"
  }
}
