# X Thread Draft

**Tweet 1**
Browser tabs shouldn't dictate battery life. Meet Sleepy Tabs Guardian -- an open-source Chrome extension that auto-sleeps idle tabs and refreshes memory hogs. #ChromeExtension #WebPerf

**Tweet 2**
Defaults: 5-minute inactivity timeout, 100 MB total heap threshold (calibrated for probe values), and separate actions for active (reminder) vs inactive (sleep) tabs. Adjustable from a slick options page with per-tab ignore toggles and instant overrides. #Productivity

**Tweet 3**
Forgetful tab hoarder? Same. Built-in memory saver kicks in whenever it wants and you can't set the timeout. Sleepy Tabs gives you the controls Chrome won't. #Chrome #MemorySaver

**Tweet 4**
When it's time to nuke the backlog, hit "Sleep all tabs" once—no more clicking every tab just to calm the browser down. #Focus #Automation

**Tweet 5**
We paired Manifest V3 with a Playwright-based companion that talks to the Chrome DevTools Protocol. Real-memory metrics, lifecycle control, native messaging -- all in TypeScript. #DevTools

**Tweet 6**
Telemetry lives on-device in IndexedDB. Check the dashboard (hello, Chrome side panel) to review every sleep/reload action and spotlight critical sites. #Privacy #DX

**Tweet 7**
Ready to calm the tab storm? Grab the repo, follow the setup guide, and help shape the roadmap. PRs and feedback welcome! _(Add repo URL)_
